An OpenAI gym environment for constrained RL in the 2D turtlebot environment

Installation
------------

Required dependencies are `gym` and the `neural_clbf` REALM toolkit.

Run `pip install -e .` to install from source